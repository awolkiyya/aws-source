version: 0.2
phases:
  install:
    runtime-versions:
      php: 8.1
    commands:
      - composer self-update
      - composer install --no-interaction --optimize-autoloader
  test:
    commands:
      - phpunit
  build:
    commands:
      - artisan migrate
      - artisan optimize
      - artisan queue:work
